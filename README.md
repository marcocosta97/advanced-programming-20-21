# Advanced Programming Assignments

This repository contains two separate assignments, each with its own requirements and deliverables.

## first_assignment

Located in `./first_assignment`

Includes:

* Java Beans assignment (Bus + CovidController + Swing dashboard)
* Java Reflection + Annotations assignment (XML serialization)
* Optional XML deserialization

See details in `first_assignment/README.md`.

## second_assignment

Located in `./second_assignment`

Includes:

* Haskell multisets (`ListBag`)
* Java Stream API Map-Reduce framework + word count (+ optional inverted index)
* Python benchmarking decorator + multithreading tests

See details in `second_assignment/README.md`.
